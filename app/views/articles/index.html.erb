<h1>All Articles HERE</h1>

<%= link_to 'Create an new article', new_article_path %>

<ul>
  <% @articles.each do |article| %>
    <li>
      <h3>
        <%= link_to article.title, article_path(article.id) %>
      </h3>
    </li>
  <% end %>
</ul>
